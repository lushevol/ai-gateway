{
	"name": "ai-gateway",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"build": "pnpm -r run build",
		"test": "pnpm -r run test",
		"dev": "pnpm -r run dev"
	},
	"devDependencies": {
		"@types/node": "^24.3.0",
		"typescript": "^5.9.2"
	}
}
